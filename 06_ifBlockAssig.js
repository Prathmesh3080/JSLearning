function ifelseHandsOn(num) {

    if (num%2==0) {
        console.log(`${num} is a Even`);
        
    } else {
        console.log(`${num} is Odd`);
    }
}
ifelseHandsOn(45);
ifelseHandsOn(70);
ifelseHandsOn(67);
ifelseHandsOn(98);  

function Kength(str) {
    var reu = str.length;
    console.log(`Length is ${reu}`);
    if (reu>=10) {
        console.log(`${str} String character length is more than 10: `);
    } else {
        console.log(`${str} String character length is less than 10: `);
    }
    
}
Kength("JavaScript-ES6");

function CHeck(para) {
    
    var ch = para.

}
CHeck("JavaScript Language");